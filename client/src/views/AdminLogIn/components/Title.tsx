import React from 'react'
import styles from './Styles.module.css'

type Props = {}

const Title = (props: Props) => {
  return (
    <h1 className={styles.Title}>LogIn To Dashboard</h1>
  )
}

export default Title